export type SfxName='treasure'|'correct'|'purchase'|'level'|'step'|'discover'|'error'|'bubble'
let ctx:AudioContext|null=null
function context(){if(typeof window==='undefined')return null;const C=(window.AudioContext||(window as any).webkitAudioContext);if(!C)return null;if(!ctx)ctx=new C();return ctx}
export function playSfx(name:SfxName,volume=.25){const c=context();if(!c)return;const map:Record<SfxName,[number,number,OscillatorType]>={treasure:[520,920,'triangle'],correct:[660,880,'sine'],purchase:[440,660,'sine'],level:[392,1046,'triangle'],step:[180,220,'sine'],discover:[330,740,'sine'],error:[190,130,'sawtooth'],bubble:[760,520,'sine']};const [a,b,type]=map[name],o=c.createOscillator(),g=c.createGain();o.type=type;o.frequency.setValueAtTime(a,c.currentTime);o.frequency.exponentialRampToValueAtTime(b,c.currentTime+.22);g.gain.setValueAtTime(Math.max(.001,volume),c.currentTime);g.gain.exponentialRampToValueAtTime(.001,c.currentTime+.28);o.connect(g);g.connect(c.destination);o.start();o.stop(c.currentTime+.3)}
export function startAmbient(volume=.08){const c=context();if(!c)return()=>{};const o=c.createOscillator(),g=c.createGain();o.type='sine';o.frequency.value=88;g.gain.value=Math.max(.001,volume);o.connect(g);g.connect(c.destination);o.start();return()=>{try{o.stop()}catch{}}}
