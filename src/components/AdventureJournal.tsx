import type {KidName,KidProgress} from '../types'
interface Props{kid:KidName;progress:KidProgress;onClose:()=>void}
export default function AdventureJournal({kid,progress,onClose}:Props){const j=progress.adventure.journal;return <main className="page-shell journal-page game-overlay-page"><header className="page-header"><button className="secondary" onClick={onClose}>← Expedition</button><div><h1>📔 {kid}'s Adventure Journal</h1><p>Story events, discoveries, treasures, science facts, and mastered knowledge.</p></div></header><section className="journal-stats"><div><span>Objectives</span><strong>{progress.adventure.completedObjectives.length}</strong></div><div><span>Map Fragments</span><strong>{progress.adventure.mapFragments.length}</strong></div><div><span>Shipwreck Rooms</span><strong>{progress.adventure.shipwreckRooms.length}</strong></div><div><span>Treasure</span><strong>{progress.adventure.treasureClaimed.length}</strong></div></section><section className="journal-list">{j.length?j.map(e=><article key={e.id}><span>{e.icon}</span><div><h3>{e.title}</h3><p>{e.text}</p><small>{new Date(e.createdAt).toLocaleString()}</small></div></article>):<div className="empty-journal">🧭 Begin exploring World 1 to fill your journal.</div>}</section></main>}
